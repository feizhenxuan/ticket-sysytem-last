<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alipay.ticketbacked.common.dal.mapper.SessionSeatMapper">

    <!-- JOIN hx_seats 获取行列号和类型
         注意：GROUP BY 不包含 ss.status，避免同一座位的重复行产生多条记录
         MAX(status) 按字母序取最严格状态：'sold' > 'locked' > 'available' -->
    <select id="findSessionSeats" resultType="java.util.LinkedHashMap">
        SELECT ss.seat_id   AS seat_id,
               s.row_num    AS `row`,
               s.col_num    AS col,
               s.seat_type  AS type,
               MAX(ss.status) AS status
        FROM hx_session_seats ss
        INNER JOIN hx_seats s ON ss.seat_id = s.id
        WHERE ss.session_id = #{sessionId}
        GROUP BY ss.seat_id, s.row_num, s.col_num, s.seat_type
        ORDER BY s.row_num, s.col_num
    </select>

    <!-- 按 session_id + seat_ids 精确释放座位，不依赖 locked_by_order_id -->
    <update id="releaseSeatsBySessionAndSeatIds">
        UPDATE hx_session_seats
        SET status = 'available', locked_by_order_id = NULL, locked_at = NULL
        WHERE session_id = #{sessionId}
          AND seat_id IN
        <foreach collection="seatIds" item="seatId" open="(" separator="," close=")">
            #{seatId}
        </foreach>
    </update>

    <!-- 按 session_id + seat_ids 精确更新座位状态（用于支付成功标记 sold） -->
    <update id="markSeatsStatusBySessionAndSeatIds">
        UPDATE hx_session_seats
        SET status = #{status}
        WHERE session_id = #{sessionId}
          AND seat_id IN
        <foreach collection="seatIds" item="seatId" open="(" separator="," close=")">
            #{seatId}
        </foreach>
    </update>

</mapper>