<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alipay.ticketbacked.common.dal.mapper.UserMapper">

    <insert id="insert" parameterType="com.alipay.ticketbacked.core.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hx_users (username, password_hash, is_active, gmt_create, gmt_modify)
        VALUES (#{username}, #{passwordHash}, #{isActive}, NOW(), NOW())
    </insert>

    <update id="updateIsActive">
        UPDATE hx_users SET is_active = #{isActive}, gmt_modify = NOW() WHERE id = #{id}
    </update>

    <select id="findAllForAdmin" resultType="com.alipay.ticketbacked.core.model.User">
        SELECT * FROM hx_users
        <where>
            <if test="isActive != null">
                is_active = #{isActive}
            </if>
            <if test="search != null and search != ''">
                AND username LIKE CONCAT('%', #{search}, '%')
            </if>
        </where>
        ORDER BY gmt_create DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>