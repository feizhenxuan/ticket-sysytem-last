<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alipay.ticketbacked.common.dal.mapper.CinemaMapper">

    <insert id="insert" parameterType="com.alipay.ticketbacked.core.model.Cinema" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hx_cinemas (name, address, longitude, latitude, phone, city, poi_id, gmt_create)
        VALUES (#{name}, #{address}, #{longitude}, #{latitude}, #{phone}, #{city}, #{poiId}, NOW())
    </insert>

    <update id="update" parameterType="com.alipay.ticketbacked.core.model.Cinema">
        UPDATE hx_cinemas
        SET name=#{name}, address=#{address}, longitude=#{longitude}, latitude=#{latitude},
            phone=#{phone}, city=#{city}, poi_id=#{poiId}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM hx_cinemas WHERE id = #{id}
    </delete>

</mapper>